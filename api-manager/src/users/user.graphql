type User @key(fields: "id") {
    id: ID!
    name: String!
    login: String!
}

extend type Query {
    User(id: ID!): User,
    Users: [User]
}

extend type Mutation {
    create(name: String!, login: String!): User
}
